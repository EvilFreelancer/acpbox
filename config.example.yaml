# Example gateway config. Copy to config.yaml and adjust.
# Every option can be overridden via environment variables (see .env.example).
# ACP uses stdio (one agent process per request); no HTTP to the agent.

acp:
  command:
    - opencode
    - acp
  env: {}
  models:
    - default
  cwd: null

gateway:
  host: "0.0.0.0"
  port: 8080
